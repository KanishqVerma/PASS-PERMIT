<nav class="navbar navbar-expand-lg bg-white shadow-sm sticky-top" style="font-size: 1.3rem">
  <div class="container-fluid px-4">
    <!-- Brand -->
    <a class="navbar-brand d-flex align-items-center gap-2" href="/">
      <img src="https://img.icons8.com/ios-filled/50/ffffff/airport.png" width="40" height="40" class="rounded-circle p-1" style="background-color: #0059b3" />
      <div class="d-flex flex-column lh-sm">
        <strong style="font-size: 1.6rem; color: #0059b3">Airport Authority of India</strong>
        <small class="text-muted" style="font-size: 1.1rem">Digital Pass Management System</small>
      </div>
    </a>

    <!-- Toggler for small screens -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNavbar" aria-controls="mainNavbar" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Center + Right Section -->
    <div class="collapse navbar-collapse" id="mainNavbar">
      <!-- Center Menu -->
      <ul class="navbar-nav mx-auto mb-2 mb-lg-0 text-center" style="gap: 1.5rem">
  <li class="nav-item">
    <a class="nav-link <%= page === 'home' ? 'active-link' : '' %>" href="/home" style="color: #0059b3">Home</a>
  </li>
  <li class="nav-item">
    <a class="nav-link <%= page === 'process' ? 'active-link' : '' %>" href="/process" style="color: #0059b3">Services</a>
  </li>
  <li class="nav-item">
    <a class="nav-link <%= page === 'about' ? 'active-link' : '' %>" href="/about" style="color: #0059b3">About</a>
  </li>
  <li class="nav-item">
    <a class="nav-link <%= page === 'contact' ? 'active-link' : '' %>" href="/contact" style="color: #0059b3">Contact</a>
  </li>

  <% if (!currUser) { %>
    <!-- Guest -->
    <li class="nav-item">
      <a class="nav-link <%= page === 'login' ? 'active-link' : '' %>" href="/login" style="color: #0059b3">Login</a>
    </li>
    <li class="nav-item">
      <a class="nav-link <%= page === 'signup' ? 'active-link' : '' %>" href="/signup" style="color: #0059b3">SignUp</a>
    </li>
  <% } %>

  <% if(currUser){ %>
    <% if (currUser.role === "user") { %>
      <li class="nav-item">
        <a class="nav-link <%= page === 'userDash' ? 'active-link' : '' %>" href="/dashboard/<%= currUser._id %>" style="color: #0059b3">
          Dashboard
        </a>
      </li>
    <% } %>
    <li class="nav-item">
      <a class="nav-link" href="/logout" style="color: #0059b3">Logout</a>
    </li>
  <% } %>
</ul>


      <% if (currUser && currUser.role === "user" && latestPass) { %>
  <div class="d-flex justify-content-lg-end justify-content-center mt-3 mt-lg-0">
    <a href="/pass/<%= latestPass._id %>/download" 
       class="btn fw-bold" 
       style="background-color: #0059b3; color: white;">
      Download Pass
    </a>
  </div>
<% } %>


    </div>
  </div>
</nav>
